program Add17Proc()
global
  proc p(in copy x:int32, out copy y:int32)
  do
    y init := x + 17
  endproc;

  var a:int32;
  var b:int32
do
  debugin a init;
  call p(a, b init);
  debugout b
endprogram
