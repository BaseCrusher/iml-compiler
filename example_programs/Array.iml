program Array()
global
  var x:int32|9|;
  var y:int32
do
  y init := 1;
  while y <= arrLen(x) do
    x[y] init := (y + 1) * (2 + 1);
    y := y + 1
  endwhile;

    y := 1;
    while y <= arrLen(x) do
        debugout x[y];
        y := y + 1
    endwhile
endprogram
